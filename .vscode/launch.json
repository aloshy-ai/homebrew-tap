{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Act Push",
      "type": "node",
      "request": "launch",
      "program": "${workspaceFolder}/node_modules/.bin/act",
      "args": ["push"],
      "console": "integratedTerminal",
      "envFile": "${workspaceFolder}/.env"
    }
  ]
}
